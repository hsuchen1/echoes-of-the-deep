# 深海迴聲 (Echoes of the Deep) - 開發紀錄與功能清單

本文件記錄了專案開發過程中，所有針對使用者體驗 (UX)、介面設計 (UI)、視覺特效與工程邏輯的重大更新與新增功能。

## 🌊 1. 沉浸式視覺與視差滾動 (Parallax & Visuals)
* **多層次視差滾動 (Multi-layer Parallax)**：將海洋背景拆分為多個圖層 (`bg-layer-0`, `bg-layer-1`, `bg-layer-2`)，在使用者向下滾動時產生具備深度感與立體感的視差錯覺。
* **無縫背景過渡**：隨著深度增加，背景圖片平滑過渡（從透光層、微光層到無光層），完美模擬下潛時光線漸暗的過程。
* **前景物件修正**：將兩叢主要的珊瑚 (`coral-1`, `coral-2`) 移至背景圖層，解決了原本向下潛時珊瑚會「往上飄走」的不自然物理現象，使其完美與海床貼合。

## 📱 2. 響應式設計與手機版優化 (RWD & Mobile UX)
* **儀表板重新佈局**：在手機版尺寸下，將數據儀表板重新排版。
  * **深度計**：移至畫面右側面板區域。
  * **環境狀態 (水壓、溫度、氧氣)**：改為在畫面底部橫向排列，大幅減少對中央海洋生物視覺的遮擋。
  * **面板微縮**：手機版自動縮減 UI 的內邊距 (Padding) 與字體大小。
* **潛艇玻璃窗寬度限制 (Viewport Contending)**：
  * **內容定位修正**：手機版（寬度小於 768px）下，主玻璃窗由於 `object-fit: fill` 導致水平方向被強烈壓縮。為解決文字內容、題目卡片被左右側金屬鋼板（Porthole Frame）遮擋與裁剪的問題，將 `.scene-content` 的最大寬度限制在 `52vw`，並向右平移 `13vw`，確保所有資訊完美浮現在透明玻璃視窗中央。
  * **文字與間距縮放**：將主視覺標題 `font-size` 調整為 `1.6rem`，並縮小英文副標題字距與字體大小，保證文字不折行也不被兩側鋼板覆蓋。
  * **結算頁面單欄化**：手機版下 `.terminal-body` 的雙欄網格自動切換成單欄，並將人物分析卡片改為垂直堆疊，完美適應窄版面。
* **微觀儀表讀數修正**：將手機版上因 `vw` 縮小而變成微小斑點的深度計大數字（`#depth-big`）與單位標籤（`.depth-unit-lbl`）改用固定的絕對字重（`22px` 與 `10px`），並隱藏手機端無法進行精細拖曳的音量滑桿（`#sound-controls`），維持 HUD 乾淨且易讀。

## 🎨 3. 電影級排版與美學 (Cinematic Typography & Aesthetics)
* **主視覺字體升級**：引入 `Noto Sans TC` 的極粗字重 (700, 900)，並為英文副標題加上極寬的字距 (`letter-spacing: 0.4em`)，打造科幻電影海報般的史詩感。
* **毛玻璃特效 (Glassmorphism)**：為按鈕、SDG 14 標籤、以及提示文字加上半透明的深色毛玻璃背景 (`backdrop-filter: blur`)，確保文字在任何明亮的海洋背景下皆具備「絕對的易讀性」。
* **引導提示優化**：
  * 新增帶有呼吸閃爍特效的音效提示：「🔊 建議戴上耳機，點擊按鈕開啟深海音效」。
  * 大幅強化了底部「▾ SCROLL TO DIVE ▾」的能見度（提高不透明度、加上深色發光陰影、加快淡入速度）。
* **左側導航線優化**：為左側的深度節點導航線加上了「水流填滿」的 CSS 動畫效果。

## 🎵 4. 深海動態音效引擎 (Deep Sea Audio Engine)
* **滾動聯動音效**：捨棄傳統的靜態背景音樂，打造了會隨「下潛深度（滾動百分比）」即時變化的音效引擎。
* **音量交錯 (Volume Crossfade)**：
  * **透光層 (淺海)**：優美壯闊的背景音樂為主。
  * **無光層 (深海)**：背景音樂逐漸變小，水壓的環境隆隆聲 (`underwater-ambience`) 逐漸變大，強化深海的孤獨與未知恐懼。
* **動態降速與降調 (Dynamic Pitch & Speed Shift)**：
  * 引擎會判斷使用者是否離開了前 25% 的透光層。
  * 進入深海後，背景音樂與環境音的**播放速度 (Playback Rate) 會開始逐漸變慢**，同時關閉音高補償 (`preservesPitch = false`)，產生時間流逝變慢、極具壓迫感的詭異變調效果。
* **UI 互動音效**：為介面按鈕加上了科幻風格的點擊音效，補足了觸覺與聽覺的回饋。
* **高相容性**：移除了會被瀏覽器嚴格 CORS 跨域政策阻擋的 Web Audio API，改採純 HTML5 Audio 屬性操作，確保在本地端 (`file://`) 也能 100% 正常播放。

## 🚀 5. 社群互動與其他工程優化
* **原生分享 API (Web Share API)**：將結算畫面的分享按鈕改寫，支援呼叫行動裝置原生的分享選單（可直接傳至 LINE、IG、Facebook 等），並會自動帶入玩家的專屬探險身分（例如：深海探勘員）。
* **資源預載 (Preload)**：在 `index.html` 的 `<head>` 中加入了關鍵背景圖的 `<link rel="preload">`，確保網頁初始載入時不會出現背景閃爍的破圖情況。

## 🐠 6. 海洋生態與環境實體設計 (Marine Life & Pollution Entities)
本專案依據不同海洋深度（透光層、弱光層、無光層）設計了對應的海洋生物與人造垃圾實體，透過純 CSS 動畫與 Canvas 粒子特效，打造極具動態與故事感的生態場景。

### 🕊️ 0. 海面 (Sea Surface)
* **海鷗 (Seagulls)**：
  * 在海面上方設計了 4 隻不同遠近大小與透明度的海鷗 (`seagull-1` 至 `seagull-4`)。
  * 使用 `swimFloat` 動畫進行垂直方向的平滑漂浮微調，部分海鷗進行水平翻轉（`scaleX(-1)`），營造自然錯落的空間感。

### ☀ 1. 透光層 (Sunlight Zone · 0-200m)
* **海豚家族 (Dolphins)**：
  * 設計了 3 隻不同大小與透明度的海豚，模擬海豚群在淺海中暢游的深度感。
  * **互動音效**：點擊帶有提示標籤的主海豚 (`#dolphin-icon`)，會即時觸發海豚高頻點擊聲的音效回饋。
* **綠蠵龜 (Green Turtle)**：
  * 設置一隻帶有微幅傾斜角度 (`rotate(-15deg)`) 的綠蠵龜，並套用 `swimFloat` 緩慢漂浮動畫，呈現悠閒游動的姿態。
* **海馬 (Seahorse)**：
  * 隱藏於底部珊瑚礁旁，套用 `jellyFloat` 進行幅度較大的上下浮動。
* **熱帶魚群 (Clownfish & Blue Tangs)**：
  * **小丑魚 (Clownfish)**：設計了 5 隻向右游動（`cf-r`）與 4 隻翻轉向左游動（`cf-l`）的小丑魚。
  * **藍倒吊 (Blue Tangs)**：設計了 4 隻向右游動（`bt-r`）與 4 隻翻轉向左游動（`bt-l`）的藍倒吊。
  * **動態魚群邏輯**：
    * 向右游動套用 `swimRight` 動畫，從畫面左側外 (`-5vw`) 移動到右側外 (`130vw`)。
    * 向左游動套用 `swimLeftFlipped` 動畫，從右側外 (`108vw`) 游至左側外 (`-120vw`)，並透過 `scaleX(-1)` 將魚頭翻轉朝左。
    * 每隻魚皆設定了獨立的尺寸（`width`）、垂直高度（`top`）、動畫執行時間（`30s` ~ `43s`）以及負值的動畫延遲時間（`animation-delay`），確保網頁載入時魚群便已交錯分佈於畫面中，而非同時從邊緣產出。

### 🌑 2. 弱光層 (Twilight Zone · 200-1000m)
* **抹香鯨 (Sperm Whales)**：
  * 包含 3 隻不同尺寸與深度的抹香鯨。主抹香鯨從左至右緩慢游過，遠景與次要抹香鯨則以反方向及低透明度呈現，呈現深海巨獸的宏偉。
  * **互動音效**：點擊主抹香鯨 (`#whale-icon`)，會播放深沉悠揚的抹香鯨歌聲 (Whale Song) 音效。
* **漂浮塑膠垃圾 (Plastic Garbage)**：
  * 分佈了 15 件不同類型與形態的漂浮塑膠垃圾（`pb-1` 至 `pb-15`），包含塑膠袋、寶特瓶、瓶蓋、塑膠繩等。
  * 每個垃圾實體皆有專屬的傾斜角度（`rotate`）、鏡像翻轉（`scale`）、不同 z-index 層級與不透明度，並搭配隨機週期的 `jellyFloat` 漂浮動畫，傳達無處不在的海洋塑料危機。
* **塑膠微粒懸浮系統 (Microplastics Canvas)**：
  * 透過 `plastic-canvas` 繪製 70 顆隨機大小與透明度的微小粒子，模擬在海中緩慢上升、擴散的塑膠微粒懸浮，提供極佳的效能表現。

### ◉ 3. 無光層 (Midnight Zone · 1000m+)
* **深海鮟鱇魚 (Anglerfish)**：
  * 設計了 3 隻面貌兇猛的鮟鱇魚，在全黑的背景中發光。
  * 使用 `fishFloat` 動畫，結合微幅的**座標位移與角度擺動**（`rotate(-1deg)` 到 `rotate(1deg)`），重現深海魚類擺動尾鰭的滯留感。
* **深海水母 (Deep-sea Jellyfish)**：
  * 5 隻深海發光水母以不同的透明度與水平鏡像分佈，使用慢速的 `jellyFloat` 呈現水母收縮噴射上升後再緩慢落下的姿態。
* **生物螢光點綴系統 (Bioluminescence Canvas)**：
  * 透過 `biolum-canvas` 動態生成 35 個帶有發光暈染效果的生物螢光點。
  * 採用**離屏畫布 (Offscreen Canvas) 預渲染發光貼圖**的優化技術，避免了每次繪圖時調用昂貴的 `shadowBlur` 濾鏡，確保在無光層滾動時幀率（FPS）穩定流暢。

## 🖥️ 7. 結算頁面 UI/UX 重構 (Terminal Scene Redesign)
* **深海高科技儀表板 (Deep Sea Sci-Fi Dashboard)**：
  * **視覺材質**：全面導入 **玻璃擬物化 (Glassmorphism)**，採用 `backdrop-filter: blur(12px)` 搭配帶有 `box-shadow` 的半透明邊框，呈現潛艇儀表板高質感的科技透明度。
  * **螢光漸層微動態**：為主標題與按鈕加上呼吸燈閃爍 (`textFlicker`)，並在身分圖示背後加入雷達擴散波紋 (`pulseHex`) 的微互動動畫。
* **響應式佈局重構**：
  * **電腦版 (Desktop)**：採用**劇院級無邊框流線 (Cinematic Centered Flow)**佈局，移除所有面板背景方框，使文字與圖示絕對置中排成單一垂直動線，宛如電影謝幕般的沉浸式極簡體驗。
  * **手機版 (Mobile)**：無縫銜接置中流線佈局，調整元素間距，並將分享與重玩按鈕鎖定於畫面底部 (`position: sticky`)，方便單手分享成績。
* **數據視覺化優化**：
  * 將知識水平排名進度條 (`.pr-bar-fill`) 改為具有科技感的左右漸層發光色帶，並讓代表玩家定位的圖示 (`🤿`) 隨著分數進度停靠在色帶末端。
  * 引進打字機效果與計數動畫數字排版，增加科技感。

## 🎬 8. 題目版面與環境音效優化 (Cinematic Hologram & Audio Tweaks)
* **劇院級無邊框浮水印 (Cinematic Hologram UI)**：
  * 將深潛過程中的「知識點」與「測驗卡片」徹底移除笨重的毛玻璃實體方框與陰影。
  * 文字採用發光漂浮字體 (Hologram Typography)，宛如潛水艇玻璃窗上的全息投影，與無邊框結算頁面風格保持一致，達成 100% 沉浸感。
  * 將選項按鈕 (`.opt-btn`) 重構為帶有科技霓虹青色左邊框與漸層光暈的漂浮文字，提升懸停互動的回饋手感。
* **海鷗與環境互動修復**：
  * 導入 `seagull.mp3` 音效，並新增海鷗專屬的點擊發聲邏輯。
  * 修正隱形標題區塊 (`z-index`) 遮擋右側海鷗點擊的問題，讓海鷗在文字「前方」飛行，創造 3D 景深感。
  * 修正中央海鷗倒退飛行的動畫方向錯誤，確保群鳥朝兩側自然四散。
* **電影感音效微調**：
  * 為了不破壞電影感的深海氛圍，將海豚高頻刺耳的叫聲降至原音量 10%，轉化為自然的深海環境微點綴。

## 🐟 9. 互動式海洋生物圖鑑 (Interactive Creature Encyclopedia)
* **全息圖鑑彈出系統**：
  * 點擊任何海洋生物或海洋垃圾，都會在該生物旁邊（而非遮蓋全畫面）浮現高科技磨砂玻璃圖鑑視窗，呈現名稱、學名、棲息深度與生態簡介。
  * 圖鑑視窗具有智慧邊緣防呆邏輯：當生物位於畫面右側或底部時，視窗會自動反向顯示，確保不超出螢幕範圍。
  * 滑鼠懸停在任何生物上，會亮起霓虹青色輪廓光暈，提示可點擊的互動提示。
* **豐富的物種資料庫**：撰寫了 10 種海洋實體的完整百科內容（海鷗、海豚、小丑魚、藍倒吊、綠蠵龜、海馬、抹香鯨、鮟鱇魚、深海水母、塑膠垃圾），每筆包含中文名、學名、棲息深度、生態特色。
* **音效與圖鑑無縫整合**：點擊海豚、鯨魚、海鷗時，圖鑑彈出的同時依然同步播放對應的環境音效。
* **多元關閉機制**：支援點擊 `[X]` 按鈕、點擊視窗外的背景，以及按下鍵盤 `Esc` 鍵三種方式關閉圖鑑。

## 🖥️ 10. 右側 HUD 儀表板風格統一 (Right HUD Style Unification)
* **全息投影 HUD 一致性**：
  * 移除了環境狀態格子（水壓、水溫、氧氣）原本的半透明深藍色背景與完整四邊框，改為透明底色搭配僅左側的霓虹青色細線，與題目卡片、結算頁面的全息投影設計語言完全統一。
  * 深度曲線圖（Canvas）與音頻波形圖的黑色底色與四邊框，改為透明底並僅保留底部細線，顯示更為輕盈。
  * `HUD 標籤` 加上了霓虹青色文字發光特效（`text-shadow`）；分隔線改為從左至右漸消的漸層設計，整體視覺更具層次感。
* **小細節修正**：
  * 將水溫的星座符號 `♒`（在部分系統顯示為色塊方形）更換為直觀的溫度計圖示 `🌡`。
  * 微調右側 HUD 面板的水平位置，讓其與右側金屬框架之間保有適當間距。
